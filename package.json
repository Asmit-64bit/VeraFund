{
  "name": "impactfund",
  "version": "1.0.0",
  "description": "> **Milestone-gated donation escrow on the blockchain.** > Donors lock funds in a smart contract. NGOs unlock them — one verified milestone at a time.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.6"
  }
}
